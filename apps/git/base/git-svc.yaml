---
apiVersion: v1
kind: Service
metadata:
  name: git-svc
  namespace: git
  annotations:
    external-dns.alpha.kubernetes.io/hostname: git.diegoluisi.eti.br
  labels:
    app: git
    version: 0.0.1
    backstage.io/kubernetes-id: git
spec:
  type: ClusterIP
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: git